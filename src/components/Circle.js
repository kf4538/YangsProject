const Circle = ({backgroundColor}) => {
    return(
        <div
            className="circle"
            style={{
                backgroundColor: backgroundColor
            }}
        
        />
    );
}

export default Circle;